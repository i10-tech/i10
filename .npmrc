# No shamefully-hoist. PSL sets it and its own Dockerfile comment records the
# cost: a hoisting change surfaces as tsc failing with TS2307, which makes a
# pnpm upgrade a build-system change. Starting strict means every import is
# declared in the package that makes it.
strict-peer-dependencies=false
auto-install-peers=true
