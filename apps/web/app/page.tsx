/*
 * Scaffold placeholder — the marketing site is NOT being built yet.
 *
 * When it is, it has a job beyond marketing: the Polar startup-program
 * application asks for a website and a hundred-word pitch, so this is a
 * Phase 1 dependency with external turnaround behind it.
 */
export default function Page() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-2xl flex-col justify-center gap-3 px-6">
      <h1 className="text-2xl font-semibold tracking-tight">i10</h1>
      <p className="text-muted-foreground text-sm">
        Transactional and human email. Keep your code, change one import.
      </p>
    </main>
  )
}
