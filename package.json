{
  "name": "i10",
  "private": true,
  "description": "i10 \u2014 transactional and human email.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:down": "docker compose -f compose.dev.yaml down",
    "dev:hosts": "bash dev/hosts.sh add",
    "dev:logs": "docker compose -f compose.dev.yaml logs -f",
    "dev:migrate": "bun --filter @i10/api db:migrate",
    "dev:reset": "docker compose -f compose.dev.yaml down -v",
    "dev:trust": "bash dev/trust-ca.sh",
    "dev:unhosts": "bash dev/hosts.sh remove",
    "dev:up": "docker compose -f compose.dev.yaml up -d --wait",
    "build": "turbo run build",
    "check-images": "bun .github/scripts/check-dockerfile-workspaces.mjs",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,mjs,md,json,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,mjs,md,json,yaml,yml}\"",
    "lint": "turbo run lint",
    "prepare": "git config core.hooksPath .githooks || true",
    "test": "turbo run test",
    "version:next": "bun .github/scripts/next-version.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@types/bun": "^1.4.2",
    "prettier": "^3.9.6",
    "turbo": "^2.10.12",
    "typescript": "6.0.3",
    "prettier-plugin-astro": "^0.14.1"
  },
  "packageManager": "bun@1.4.2",
  "trustedDependencies": [
    "esbuild",
    "vue-demi"
  ],
  "overrides": {
    "@react-email/ui": {
      "next": "^16.3.3"
    }
  }
}
